<h1 class="my-5 text-center text-danger">TOUTES NOS MARQUES</h1>


<div class="container text-center">
  <div class="row row-cols-lg-3 row-cols-md-2 row-cols-1 g-3">

    <% @brands.each do |brand| %>
        <div class="col" id="brand_<%= brand.id %>"%>
          <%= link_to brand_path(brand), class: "text-decoration-none" do %>

            <div class="card bg-warning text-white h-100">

              <div class="card-body">
                <p class="card-text"><%= brand.name %></p>
              </div>

            </div>

          <% end %>
          <%# <%= link_to 'supprimer', category_path(category), class: 'btn btn-danger', data: {turbo_method: :delete} %>
        </div>
    <% end %>

  </div>
</div>
