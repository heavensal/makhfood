<h1 class="text-center my-5">Mon Devis</h1>

<table class="table table-primary table-striped w-75 mx-auto">

  <thead class="w-75 mx-auto">
    <tr class="table-secondary">
      <td>
        CATEGORIE
      </td>
      <td>
        MARQUE
      </td>
      <td>
        PRODUIT
      </td>
      <td>
        VENDU PAR
      </td>
      <td>
        QUANTITE
      </td>
    </tr>
  </thead>

<% @quote_items.each do |item| %>
  <tr class="table-primary">
    <td>
      <%= item.product.category.name %>
    </td>
    <td>
      <%= item.product.brand.name %>
    </td>
    <td>
      <%= item.product.name %>
    </td>
    <td>
      <%= item.product.box %>
    </td>
    <td>
      <%= form_with model: item, url: quote_item_path(item), method: :patch, local: true do |f| %>
        <%= f.number_field :quantity, value: item.quantity %>
        <%= f.submit "Mettre à jour" %>
      <% end %>
    </td>
  </tr>
<% end %>








</table>
